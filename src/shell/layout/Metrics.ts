module shell.layout {

  export interface Metrics {
    hostWidth: number;
    hostHeight: number;
    emWidth: number;
    emHeight: number;
    scrollbarWidth: number;
    scrollbarHeight: number;
  }

}